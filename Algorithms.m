%An algorithm is the sequence of steps needed to solve a problem
%SCRIPTS- matlab programs 
%sequence of instructions that is stored and saved in a file
%input statements reads in values from that the user enters

%Example

%this script calculates the area of the circle

radius = 7;
area = ceil(pi*radius^2);

%input statement

rad = input('Enter the radius of the circle: ');
%value = input(rad);
area = ceil(pi*rad ^2);
